import * as React from 'react';

// The home page is just a larger search box. Nothing special.
// At the moment this just exists to prevent an infinite redirect in AppRouter

class Home extends React.Component<{}> {

    public render() {
        return (
            null
        )
    }
}

export default Home